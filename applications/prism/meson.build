prism_themes = ['default', 'storm', 'light']
prism_make = join_paths(meson.current_source_dir(), 'make.js')
prism_dir = join_paths(datadir, 'prism-launcher')

foreach theme : prism_themes
  subdir(theme)
endforeach
